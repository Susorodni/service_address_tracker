[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "service-address-tracker"
version = "0.1.0"
description = "CEG LSLRP internal application for tracking service addresses"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "pandas",
    "openpyxl"
]